export enum AppRoute {
  Root = '/',
  Any = '/*',
  Login = '/login',
  Registration = '/registration',
  Product = '/product',
  Products = '/products'
}

export enum AuthStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN'
}
